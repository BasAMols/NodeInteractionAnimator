import { WindowPanel } from './window';

export class ImportPanel extends WindowPanel {
    constructor() {
        super('import', 'Import');
    }
    public resize(): void {
        super.resize();
    }
}